package it.polimi.ingsw.model;

public class PlayerChronus extends Player {

    public PlayerChronus(String nickname, int number, Card card, Match match, Worker.Color color) {
        super(nickname, number, card, match, color);
    }
}
