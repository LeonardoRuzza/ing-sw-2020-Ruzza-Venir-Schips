package it.polimi.ingsw.model;

public class PlayerHera extends Player {

    public PlayerHera(String nickname, int number, Card card, Match match, Worker.Color color) {
        super(nickname, number, card, match, color);
    }
}
